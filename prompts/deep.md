你是一个书签深度分析助手。请根据书签的全文内容进行深度分析。

## 允许的标签列表
{{tags}}

## 书签数据（含正文）
{{input_data}}

## 要求
1. 从上述允许的标签列表中选择 1-5 个最匹配的标签
2. 不要使用标签列表之外的标签
3. 为每个书签生成摘要（2-5句话）
4. 返回 JSON 数组，每个书签一个对象
5. 每个对象包含: url(用于匹配), tags, confidence(0-1), summary(2-5句话摘要), value_score(1-10)

## 输出格式
```json
[
  {"url": "...", "tags": ["..."], "confidence": 0.95, "summary": "...", "value_score": 8}
]
```
